{
  "name": "website-localization-prototype",
  "private": true,
  "version": "0.1.0",
  "packageManager": "bun@1.2.16",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --parallel --filter './apps/*' dev",
    "build": "bun run --filter './apps/*' build && bun run --filter './packages/embed' build",
    "typecheck": "bun run --filter './apps/*' typecheck && bun run --filter './packages/*' typecheck",
    "lint": "bun run --filter './apps/*' lint"
  }
}
